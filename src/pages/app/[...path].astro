---
import SpaRoot from '../../components/SpaRoot.astro';

// Тот же SPA, что и на /login: сервер отдаёт оболочку для /app и любого вложенного пути,
// поэтому перезагрузка и прямые ссылки работают. Роут намеренно on-demand —
// на пререндеренном маршруте не выполнился бы middleware с проверкой сессии.
---

<SpaRoot title="Личный кабинет — NeyroHub" />
